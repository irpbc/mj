
@LogAfter
@LogAfter
int main(int argc, long arvPtr) {
    hello();
    
    printf("Enter an integer: ");
    int broj = scan_int();
    printf("You entered: %d\n", broj);
    
    if (broj < 1) {
        return 1;
    }
   
    for (int i = 1; i <= 5; i++) {
        for (int j = 1; j <= 5; j++) {
            printf("i * j = %d\ti + j = %d\n", i * j, i + j);
        }
    }
    return 0;
}